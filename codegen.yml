overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: null
generates:
  generated/graphql-backend.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"  
      
      

    # this config fix the issue on incompatible types between code gen generated resolvers and  appolo server resolver thats is graphql.ts
    #config:
      #useIndexSignature: true

